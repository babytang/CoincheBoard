<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <ListPreference
        android:defaultValue="0"
        android:entries="@array/pref_coinche_score_list"
        android:entryValues="@array/pref_coinche_score_values"
        android:key="coinche_score_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_coinche_score" />

    <ListPreference
        android:defaultValue="3"
        android:entries="@array/pref_coinche_coeff_list"
        android:entryValues="@array/pref_coinche_coeff_values"
        android:key="coinche_coeff_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_coinche_coeff" />
    
</PreferenceScreen>